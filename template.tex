$if(header-includes)$
$header-includes$
$endif$
\documentclass[$fontsize$]{article}

\usepackage{fontspec}

% 1. Define the emoji fallback font (must be installed on your system)
% Try Noto Color Emoji, Segoe UI Emoji (Windows), or Apple Color Emoji (macOS)
\directlua{luaotfload.add_fallback ("emojifallback", { "Noto Color Emoji:mode=harf;" } )}

% 2. Set your main font and enable the fallback feature
\setmainfont{DejaVu Sans}[
  % Replace DejaVu Sans with your preferred main font for the resume
  RawFeature={fallback=emojifallback}
]

% Ensure the titles are also set using a font that supports the fallback
\setmonofont{DejaVu Sans Mono}[
  RawFeature={fallback=emojifallback}
]

\usepackage{geometry}
\geometry{$geometry$}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage[colorlinks=true, urlcolor=blue]{hyperref}

% Remove page number
\pagestyle{empty}

% Customize section headings
\titlespacing*{\section}{0pt}{1.5ex}{0.8ex}
\setcounter{secnumdepth}{-2}
\titleformat{\section}
  {\Large\bfseries\raggedright}{\thesection}{0em}{\MakeUppercase}

% Set header style
\usepackage{fancyhdr}
\usepackage{titlesec} % Ensure titlesec is still loaded

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% --- HEADER DEFINITION ---
\pagestyle{fancy}
\fancyhead{} % Clear all header fields
\fancyfoot{} % Clear all footer fields

% 1. Place the Author Name in the Center of the header (C)
\fancyhead[C]{
  \huge\textbf{$author$} \\
  \normalsize $jobtitle$
}

% 2. Use the 'L' and 'R' sections for the header-info
% NOTE: This is complex. A better approach is often to skip fancyhdr for this specific need.
% We will redefine the entire header structure below to fix the overlap.

\fancypagestyle{plain}{ % Redefine the 'plain' style (used for page 1)
    \fancyhead{}
    \fancyfoot{}
    \fancyhead[C]{
      \huge\textbf{$author$} \\
      \normalsize $jobtitle$
    }
    \fancyhead[R]{} % Clear the right side for the name

    % Now, add the header-info below the rule, NOT using fancyhdr
    \renewcommand{\headrulewidth}{0pt}
}
\pagestyle{plain} % Apply this style to the entire document

% Use a dedicated title/maketitle setup to control the top block:

\titleformat{\section}
  {\Large\bfseries\raggedright}{}{0em}{\MakeUppercase} % The second argument is now EMPTY {}
\titlespacing*{\section}{0pt}{1.5ex}{0.8ex}

% Disable hyphenation globally
\tolerance=10000
\hyphenpenalty=10000
\exhyphenpenalty=10000

\begin{document}

% Add the Header Info block right here, outside the fancyhdr system
\begin{center}
    \normalsize $contact$ \\
    \normalsize $social$
\end{center}
\hrule % Horizontal rule below the contact details

% Now the body content follows:
$body$

\end{document}
